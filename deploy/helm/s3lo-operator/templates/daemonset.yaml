apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ include "s3lo-operator.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "s3lo-operator.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "s3lo-operator.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "s3lo-operator.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      serviceAccountName: {{ include "s3lo-operator.serviceAccountName" . }}
      hostNetwork: true
      containers:
        - name: s3lo-proxy
          image: {{ include "s3lo-operator.image" . }}
          securityContext:
            runAsUser: 0
          env:
            - name: S3LO_PORT
              value: {{ .Values.proxy.port | quote }}
            - name: S3LO_CERTS_DIR
              value: /host/etc/containerd/certs.d
            - name: S3LO_PRESIGN_TTL
              value: {{ .Values.proxy.presignTTL | quote }}
            {{- if .Values.signing.enabled }}
            - name: S3LO_VERIFY_SIGNATURES
              value: "true"
            - name: S3LO_KEY_REF
              value: {{ .Values.signing.keyRef | quote }}
            {{- end }}
          ports:
            - containerPort: {{ .Values.proxy.port }}
              hostPort: {{ .Values.proxy.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.proxy.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: {{ .Values.proxy.port }}
            initialDelaySeconds: 2
            periodSeconds: 5
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: containerd-certs
              mountPath: /host/etc/containerd/certs.d
            {{- if and .Values.signing.enabled .Values.signing.keySecret.name }}
            - name: signing-key
              mountPath: /etc/s3lo/signing
              readOnly: true
            {{- end }}
      volumes:
        - name: containerd-certs
          hostPath:
            path: /etc/containerd/certs.d
            type: DirectoryOrCreate
        {{- if and .Values.signing.enabled .Values.signing.keySecret.name }}
        - name: signing-key
          secret:
            secretName: {{ .Values.signing.keySecret.name }}
            items:
              - key: {{ .Values.signing.keySecret.key }}
                path: {{ .Values.signing.keySecret.key }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
